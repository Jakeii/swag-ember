<div class="swag-icon" style={{style}}>
  <circle></circle>
  <div class="swagifact-name">
    {{item.name}}
  </div>
</div>
{{! these are seperate so that the meta popover is always on top of all of the icons }}
<div class="swagifact-meta" style={{style}}>
  <ul>
    <li><strong>description:</strong> <p>{{item.description}}</p></li>
    <li><strong>completed:</strong> {{completed}}</li>
    <li><strong>requires:</strong>
      <ul class="requires">
        {{#each requires as |skill|}}
        <li class="{{if skill.completed 'completed'}}">{{skill.name}}</li>
        {{/each}}
      </ul>
    </li>
    <li><strong>provides:</strong>
      <ul class="provides">
        {{#each provides as |skill|}}
        <li class="{{if skill.completed 'completed'}}">{{skill.name}}</li>
        {{/each}}
      </ul>
    </li>
    <li><strong>Send Verb to Learninglocker</strong>:
      <ul>
        <li><button type="button small" {{action 'sendCompleted'}}>Completed</button></li>
      </ul>
    </li>
  </ul>
</div>
